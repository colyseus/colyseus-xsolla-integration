import { type AuthSettings } from "@colyseus/auth";

export const authSettings: Partial<AuthSettings> = {
    /**
     * TODO: configure your own auth hooks
     * 
     * Read more: https://docs.colyseus.io/auth/module#backend-api
     */
};